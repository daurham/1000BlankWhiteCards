import React from 'react';
import styled from "styled-components";

export default function TurnTracker() {
  const users = ['Alvina', 'Jini', 'Trevor', 'Waylon'];
  return(
    <div>this is TurnTracker</div>
  );
}
